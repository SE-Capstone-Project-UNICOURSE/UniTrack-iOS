//
//  Colors.swift
//  Finance
//
//  Created by UI8 on 13/10/2020.
//

import SwiftUI

extension Color {
    //Colors that we have added to Assets Catalog are accessible from here. You can add additional colors by typing static let colorName = Color("colorName"). Please be sure to add new colors to the Assets Catalog first. You can also use this for systemColors by typing static let colorName = Color(.systemOrange).
    static let listRowBackgroundColor1 = Color("ListRowBackground1")
    static let listRowBackgroundColor2 = Color("ListRowBackground2")
    static let notificationIndicator = Color("NotificationIndicator")
}
